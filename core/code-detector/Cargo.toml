[package]
name = "code-detector"
version = "0.1.0"
edition = "2021"
description = "Content-based file type detection for code files"
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.0"
