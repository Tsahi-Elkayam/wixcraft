[package]
name = "wix-uninstall"
version = "0.1.0"
edition = "2021"
description = "Uninstall script generator and product finder for MSI packages"
license = "MIT"

[[bin]]
name = "wix-uninstall"
path = "src/main.rs"

[lib]
name = "wix_uninstall"
path = "src/lib.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
