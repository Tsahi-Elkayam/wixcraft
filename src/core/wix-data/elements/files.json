{
  "$schema": "../schema/element.schema.json",
  "name": "Files",
  "namespace": "wix",
  "since": "v5",
  "description": "Includes multiple files using wildcards. Replaces the need for Heat harvesting in many cases.",
  "documentation": "https://wixtoolset.org/docs/schema/wxs/files/",
  "parents": ["Component", "ComponentGroup", "Directory", "DirectoryRef", "StandardDirectory"],
  "children": [],
  "attributes": {
    "Include": {
      "type": "string",
      "required": true,
      "description": "Wildcard pattern for files to include (e.g., 'bin\\**\\*.dll')."
    },
    "Exclude": {
      "type": "string",
      "required": false,
      "description": "Wildcard pattern for files to exclude."
    }
  },
  "examples": [
    {
      "description": "Include all DLLs from bin folder",
      "code": "<Files Include=\"bin\\**\\*.dll\" />"
    },
    {
      "description": "Include all files except PDBs",
      "code": "<Files Include=\"dist\\**\\*\" Exclude=\"**\\*.pdb\" />"
    }
  ]
}
