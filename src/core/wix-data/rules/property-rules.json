{
  "$schema": "../schema/rule.schema.json",
  "rules": [
    {
      "id": "property-uppercase",
      "name": "Public properties must be UPPERCASE",
      "description": "Properties that need to persist or be passed to server must be UPPERCASE",
      "severity": "warning",
      "element": "Property",
      "condition": "attributes.Id !== attributes.Id.toUpperCase() && !attributes.Secure",
      "message": "Property '{{attributes.Id}}' should be UPPERCASE to be public. Lowercase properties are private."
    },
    {
      "id": "property-secure-recommended",
      "name": "Sensitive properties should be Secure",
      "description": "Properties containing sensitive data should be marked Secure to avoid logging",
      "severity": "info",
      "element": "Property",
      "condition": "isSensitivePropertyName(attributes.Id) && attributes.Secure !== 'yes'",
      "message": "Property '{{attributes.Id}}' appears to contain sensitive data. Consider adding Secure=\"yes\"."
    },
    {
      "id": "property-hidden-ui",
      "name": "Properties hidden from UI should be marked Hidden",
      "description": "Use Hidden=\"yes\" to prevent property from appearing in logs and UI",
      "severity": "info",
      "element": "Property",
      "condition": "isSensitivePropertyName(attributes.Id) && attributes.Hidden !== 'yes'",
      "message": "Consider adding Hidden=\"yes\" to Property '{{attributes.Id}}' to hide from logs."
    },
    {
      "id": "property-avoid-hardcoded-paths",
      "name": "Avoid hardcoded paths in properties",
      "description": "Hardcoded paths like C:\\ may not work on all systems",
      "severity": "warning",
      "element": "Property",
      "condition": "attributes.Value && /^[A-Za-z]:\\\\/.test(attributes.Value)",
      "message": "Property '{{attributes.Id}}' contains a hardcoded path. Use directory references or formatted strings instead."
    }
  ]
}
